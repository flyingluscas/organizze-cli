#!/usr/bin/env node

const commander = require('commander')
const { version } = require('../package')

commander
  .version(version)
  .description('Manage your finances using Organizze and your best friend the terminal')

commander.parse(process.argv)
